#!/bin/bash
CREDFILE=/nds/config/openstack-creds.sh
if [ -s ${CREDFILE} ] ; then
    . ${CREDFILE} 
    python /usr/local/bin/deploy.py 
    exit 0
else
    cat > ${CREDFILE}
    #debugging
    cat ${CREDFILE}
    env
    . ${CREDFILE}
    env
fi
