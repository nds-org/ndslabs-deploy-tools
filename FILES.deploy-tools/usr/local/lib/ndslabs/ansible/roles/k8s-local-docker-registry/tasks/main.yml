
- name: Deploy registry RC, SVC, and proxy DS
  local_action: command {{ kubectl }} create -f {{ role_path }}/files/{{ item }}
  with_items:
    - docker-registry-rc.yaml
    - docker-registry-svc.yaml
    - docker-registry-proxy-ds.yaml  

