---
- name: NDSLabs add user
  user: name=ndsuser groups=admin,sudo generate_ssh_key=yes ssh_key_bits=2048

- name: NDS set user password
  shell: echo ndsuser:_-0changme | sudo chpasswd
  no_log: True

- user: name=ndslabs groups=sudo,ndslabs

#sudo useradd -p "*" -U -m user1 -G sudo
