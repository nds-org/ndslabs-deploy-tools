#
# Cluster
#
[all:vars]
logical_cluster_name=example
cluster_name=cluster.local
native_dns=os.ncsa.edu
image=CoreOSAlpha
flavor=m1.medium
kube_version=1.2.4
key_name={{logical_cluster_name}}
ndslabs_domain= {{logical_cluster_name}}.ndslabs.org

#
# Docker images
#
apiserver_image_name=ndslabs/apiserver:latest
webui_image_name=ndslabs/angular-ui:latest
loadbal_image_name=gcr.io/google_containers/nginx-ingress-controller:0.8.2
gluster_image_name=davidraila/gluster:1.0.1-alpha.NDS-395 

#
# Logging and monitoring
# 
#enablekibana=true
enablegrafana=true
#enablepromdash=true
#enablekubeui=true
enablekubedash=true

#
# Servers 
#
[masters]
master1

[etcd:children]
masters

[glfs]
gfs[1:4]

[compute]
node1

[monitor]
lma

[loadbal]
loadbal

#
# Groups - assigned according to roles
#

#Compute nodes
[nodes:children]
glfs
compute
loadbal
monitor

#all nodes
[cluster:children]
nodes
masters
etcd

[all:children]
cluster

[openstack:children]
cluster
[coreos:children]
cluster
[publicip:children]
loadbal

#
# Cluster-Specific Group Variables/overrides
#
[glfs:vars]
vol_size=200
vol_name= brick0
brick_mount_path= /media/brick0
service_name=media-brick0.mount

