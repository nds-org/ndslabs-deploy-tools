#
# Cluster
#
[all:vars]
logical_cluster_name=example
cluster_name=cluster.local
native_dns=os.ncsa.edu
image=CoreOSAlpha
flavor=m1.small
kube_version=1.2.4
key_name={{logical_cluster_name}}
ndslabs_domain= {{logical_cluster_name}}

#
# Docker images
#
apiserver_image_name=ndslabs/apiserver:latest
webui_image_name=ndslabs/ndslabs-gui:latest
loadbal_image_name=ndslabs/nginx-ingress-controller:latest
gluster_image_name=ndslabs/gluster:latest

#
# Logging and monitoring
# 
#enablegrafana=true
#enablepromdash=true
#enablekibana=true
#enablekubedash=true
#enablekubeui=true

#
# Servers 
#
[masters]
develop

[etcd:children]
masters

[loadbal:children]
dev

[compute:children]
dev

#[glfs]
#dev

[dev]
develop

#
# Groups - assigned according to roles
#

#Compute nodes
[nodes:children]
loadbal
compute
#glfs

#all nodes
[cluster:children]
nodes
masters
etcd

[all:children]
cluster

[openstack:children]
cluster
[coreos:children]
cluster
[publicip:children]
dev

#
# Cluster-Specific Group Variables/overrides
#


