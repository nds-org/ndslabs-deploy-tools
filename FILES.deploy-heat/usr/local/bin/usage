#!/bin/bash

cat << EOF
    Usage:
    docker create --name deploy -it deploy-heat
    docker cp <your openstack-rc.sh>  deploy:/nds/config/openstackrc.sh
    docker start -ai  --name nds-deploy
EOF
exit 0
